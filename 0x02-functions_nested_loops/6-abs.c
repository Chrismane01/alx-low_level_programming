#include "main.h"
/**
 * _abs - This is the function that prints absolute
 * value of a number
 *
 * @d: integer input
 *
 * Return: Absolute value of number d
 */
int _abs(int d)
{
if (d >= 0)
{
return (d);
}
else
{
return (d * -1);
}
}
